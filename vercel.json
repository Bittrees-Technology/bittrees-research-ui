{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/gate/(.*)", "destination": "/api/gate?p=$1" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
